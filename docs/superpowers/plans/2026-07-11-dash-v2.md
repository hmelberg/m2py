# Dash v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bygg dash v2 (objektbasert dashboard med interact-modell) i safestat og slett den gamle dashboard-motoren, per spec `docs/superpowers/specs/2026-07-11-dash-v2-design.md`.

**Architecture:** Språknøytral motor `js/dash.js` (mosaikk-layout, kort, widgets, payload-rendering; ren halvdel Node-testet, DOM-halvdel browser-only) + tynn Brython-adapter `brython/dash.py` (bruker-API `dashboard()/add()/controls()`, type-dispatch, widget-inferens). All data krysser Python↔JS-grensen som JSON-strenger; kun callbacks (funksjoner) og DOM-noder sendes rått. Gammel `js/dashboard.js` + visningskoblinger slettes.

**Tech Stack:** Vanilla JS (UMD-mønster som `js/dashboard.js`), Node innebygd testrunner (`node --test`), Brython, Plotly (allerede lastet), markdown-it (allerede lastet), CSS grid med appens temavariabler.

## Global Constraints

- Ingen nye avhengigheter, ingen `package.json`/npm — tester kjøres med `node --test tests/js/<fil>.test.js`.
- `js/dash.js` bruker samme UMD-idiom som `js/dashboard.js:530-532`: IIFE + `module.exports = D` + `global.Dash = D`. Ren halvdel øverst (Node-testbar, ingen DOM), DOM-halvdel nederst.
- `brython/dash.py` bygger ALDRI kort/layout-DOM selv — alt går via `window.Dash.*` (spec §3.2, suksesskriterium 4).
- CSS bruker kun appens temavariabler (`--bg-panel`, `--border`, `--text`, `--text-muted`, `--accent`, `--table-grid`, `--table-header-bg`) og `color-mix(in srgb, var(--accent) N%, transparent)` for aksent-toner. Ingen hardkodede farger.
- Maks 12 kolonner i mosaikk; `.` = tom celle; områder må være rektangulære (spec §6.1).
- Arbeid på `master` i safestat. Commit per task. Ikke push før alt er verifisert (Task 8).
- Eksempel-tekster på norsk.

## Filkart

| Fil | Ansvar |
|---|---|
| `js/dash.js` (ny) | Motor: mosaikk-parsing, auto-layout-plan (ren halvdel); mount, kort, widgets, payload-rendering, debounce (DOM-halvdel) |
| `tests/js/dash.test.js` (ny) | Node-tester for ren halvdel |
| `css/dash.css` (ny) | Alt dash-utseende, temavariabel-basert |
| `brython/dash.py` (ny) | Bruker-API, type-dispatch, widget-inferens, funksjons-rekjøring, stdout-fangst |
| `js/brython-engine.js` (endres) | Én ny `LIB_REGISTRY`-oppføring: `dash` |
| `index.html` (endres) | +`<link css/dash.css>`; −alle gamle dashboard-koblinger |
| `examples/bry10_dashboard_iris.txt`, `examples/bry11_dashboard_salg.txt`, `examples/ex_dashboard_iris.py`, `examples/ex_dashboard_iris.txt` (omskrives) | Nye API-eksempler |
| `js/dashboard.js`, `css/dashboard.css`, `tests/js/dashboard.test.js` (slettes) | Gammel motor |

---

### Task 1: `js/dash.js` ren halvdel — mosaikk-parser

**Files:**
- Create: `js/dash.js`
- Test: `tests/js/dash.test.js`

**Interfaces:**
- Produces: `D.parseMosaic(str)` → `{columns, rows, names: [str], gridTemplateAreas: '"a b" "c d"'}` eller `{error: 'melding med linjenr'}`. Brukes av Task 3 (DOM) og testes her.

- [ ] **Step 1: Skriv failende test**

Opprett `tests/js/dash.test.js`:

```js
const test = require('node:test');
const assert = require('node:assert');
const D = require('../../js/dash.js');

test('parseMosaic: enkel 2x2', () => {
  const m = D.parseMosaic('a b\nc d');
  assert.strictEqual(m.error, undefined);
  assert.strictEqual(m.columns, 2);
  assert.strictEqual(m.rows, 2);
  assert.deepStrictEqual(m.names.sort(), ['a', 'b', 'c', 'd']);
  assert.strictEqual(m.gridTemplateAreas, '"a b" "c d"');
});

test('parseMosaic: spenn horisontalt og vertikalt', () => {
  const m = D.parseMosaic(`
    kpi1 kpi2 kpi3 kpi3
    plot plot plot tab
    plot plot plot tab
  `);
  assert.strictEqual(m.error, undefined);
  assert.strictEqual(m.columns, 4);
  assert.strictEqual(m.rows, 3);
  assert.ok(m.names.includes('plot'));
});

test('parseMosaic: punktum er tom celle', () => {
  const m = D.parseMosaic('a . b\na . b');
  assert.strictEqual(m.error, undefined);
  assert.deepStrictEqual(m.names.sort(), ['a', 'b']);
  assert.strictEqual(m.gridTemplateAreas, '"a . b" "a . b"');
});

test('parseMosaic: ikke-rektangulaert omraade gir feil', () => {
  const m = D.parseMosaic('a a b\nc a b');
  assert.match(m.error, /rektangul/);
  assert.match(m.error, /"a"/);
});

test('parseMosaic: ulikt antall kolonner per linje gir feil', () => {
  const m = D.parseMosaic('a b c\nd e');
  assert.match(m.error, /linje 2/);
});

test('parseMosaic: mer enn 12 kolonner gir feil', () => {
  const m = D.parseMosaic('a b c d e f g h i j k l m');
  assert.match(m.error, /12/);
});

test('parseMosaic: tom streng gir feil', () => {
  assert.ok(D.parseMosaic('').error);
  assert.ok(D.parseMosaic(null).error);
});
```

- [ ] **Step 2: Kjør testen — skal feile**

Kjør: `node --test tests/js/dash.test.js`
Forventet: FAIL (`Cannot find module '../../js/dash.js'`)

- [ ] **Step 3: Implementer parseren**

Opprett `js/dash.js`:

```js
/* dash.js — dash v2-motor (spec: docs/superpowers/specs/2026-07-11-dash-v2-design.md)
   Ren halvdel (øverst): mosaikk-parsing + auto-layout-plan. Node-testet, ingen DOM.
   DOM-halvdel (nederst): mount, kort, widgets, payload-rendering. Kun browser.
   Adaptere (brython/dash.py m.fl.) kaller det globale `Dash`-API-et; all data
   krysser grensen som JSON-strenger, pluss rå callbacks og DOM-noder. */
(function (global) {
  'use strict';
  var D = {};

  // ---------- ren halvdel ----------

  D.parseMosaic = function (str) {
    if (!str || !String(str).trim()) return { error: 'layout er tom' };
    var rows = String(str).split('\n')
      .map(function (l) { return l.trim(); })
      .filter(function (l) { return l.length; })
      .map(function (l) { return l.split(/\s+/); });
    var cols = rows[0].length;
    if (cols > 12) return { error: 'layout: maks 12 kolonner, fikk ' + cols };
    for (var i = 0; i < rows.length; i++) {
      if (rows[i].length !== cols) {
        return { error: 'layout linje ' + (i + 1) + ': ventet ' + cols +
                 ' navn, fikk ' + rows[i].length };
      }
    }
    var box = {};
    rows.forEach(function (row, r) {
      row.forEach(function (name, c) {
        if (name === '.') return;
        var b = box[name] || (box[name] = { r0: r, r1: r, c0: c, c1: c });
        if (r < b.r0) b.r0 = r; if (r > b.r1) b.r1 = r;
        if (c < b.c0) b.c0 = c; if (c > b.c1) b.c1 = c;
      });
    });
    for (var name in box) {
      var b = box[name];
      for (var r = b.r0; r <= b.r1; r++) {
        for (var c = b.c0; c <= b.c1; c++) {
          if (rows[r][c] !== name) {
            return { error: 'layout: omraadet "' + name +
                     '" er ikke rektangulaert (linje ' + (r + 1) + ')' };
          }
        }
      }
    }
    return {
      columns: cols,
      rows: rows.length,
      names: Object.keys(box),
      gridTemplateAreas: rows.map(function (row) {
        return '"' + row.join(' ') + '"';
      }).join(' ')
    };
  };

  if (typeof module !== 'undefined' && module.exports) module.exports = D;
  global.Dash = D;
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 4: Kjør testen — skal passere**

Kjør: `node --test tests/js/dash.test.js`
Forventet: PASS, 7 tester.

- [ ] **Step 5: Commit**

```bash
git add js/dash.js tests/js/dash.test.js
git commit -m "feat(dash): mosaikk-parser (ren halvdel av dash v2-motoren)"
```

---

### Task 2: `js/dash.js` ren halvdel — auto-layout-plan

**Files:**
- Modify: `js/dash.js` (legg til før UMD-halen)
- Test: `tests/js/dash.test.js` (append)

**Interfaces:**
- Produces: `D.autoSpan(kind, cols)` → int 1–12 (kolonnespenn i 12-kol-grid); `D.autoOrder(kind)` → 0 for KPI (`number`), ellers 1. Brukes av DOM-halvdelen (Task 3) når ingen mosaikk er angitt. `cols` er antall tabellkolonner (kun relevant for `kind === 'table'`).

- [ ] **Step 1: Skriv failende tester (append i `tests/js/dash.test.js`)**

```js
test('autoSpan: KPI 3, plott/bilde 6, tekst/markdown 12', () => {
  assert.strictEqual(D.autoSpan('number'), 3);
  assert.strictEqual(D.autoSpan('figure'), 6);
  assert.strictEqual(D.autoSpan('image'), 6);
  assert.strictEqual(D.autoSpan('markdown'), 12);
  assert.strictEqual(D.autoSpan('text'), 12);
});

test('autoSpan: tabell 6 ved faa kolonner, 12 ved mange', () => {
  assert.strictEqual(D.autoSpan('table', 4), 6);
  assert.strictEqual(D.autoSpan('table', 9), 12);
  assert.strictEqual(D.autoSpan('table'), 6);
});

test('autoOrder: KPI foerst', () => {
  assert.strictEqual(D.autoOrder('number'), 0);
  assert.strictEqual(D.autoOrder('figure'), 1);
});
```

- [ ] **Step 2: Kjør — skal feile** (`D.autoSpan is not a function`)

- [ ] **Step 3: Implementer (i `js/dash.js`, rett etter `parseMosaic`)**

```js
  D.autoSpan = function (kind, cols) {
    if (kind === 'number') return 3;
    if (kind === 'markdown' || kind === 'text') return 12;
    if (kind === 'table') return (cols && cols > 6) ? 12 : 6;
    return 6; // figure, image, node, error
  };

  D.autoOrder = function (kind) {
    return kind === 'number' ? 0 : 1;
  };
```

- [ ] **Step 4: Kjør — PASS (10 tester)**

- [ ] **Step 5: Commit**

```bash
git add js/dash.js tests/js/dash.test.js
git commit -m "feat(dash): auto-layout-plan (spenn og rekkefolge per korttype)"
```

---

### Task 3: `js/dash.js` DOM-halvdel — motor-API

**Files:**
- Modify: `js/dash.js` (DOM-halvdel mellom ren halvdel og UMD-halen)

**Interfaces:**
- Consumes: `D.parseMosaic`, `D.autoSpan`, `D.autoOrder` (Task 1–2); globale `markdownit`, `Plotly` (finnes i appen); `#outputArea` (index.html:280).
- Produces (kalles fra `brython/dash.py`, Task 5 — signaturene MÅ stemme):
  - `Dash.create(optsJson)` → `dashId` (string). opts: `{title, layout}` (layout kan være null).
  - `Dash.addCard(dashId, optsJson, onChange, nodeEl)` → `cardId` (string). opts: `{title, area, controls, content}` — alle valgfrie (`unit` finnes IKKE her; den bakes inn i payloaden av Python-siden); `controls` er liste av widget-specs (se under); `content` er payload eller null (funksjonskort får innhold via `updateCard`); `onChange` er funksjon eller null; `nodeEl` er DOM-node eller null (brukes når `content.kind === 'node'`).
  - `Dash.updateCard(cardId, payloadJson, nodeEl)` → void.
  - `Dash.addControls(dashId, specsJson, onChange)` → void (toppstripe).
  - `onChange` kalles alltid med ÉN JSON-streng: `{"navn": råverdi, ...}` — slider/numberfield → tall, checkbox → bool, dropdown → **valgt indeks** (int), textfield → streng.
  - Widget-spec-format (JSON): `{type:'slider', name, min, max, step, default, label}` · `{type:'dropdown', name, options:[str], index, label}` · `{type:'checkbox', name, default, label}` · `{type:'textfield', name, default, label}` · `{type:'numberfield', name, default, min, max, step, label}`.
  - Payload-format: `{kind:'markdown', text}` · `{kind:'text', text}` · `{kind:'number', value, unit}` · `{kind:'table', html, cols}` · `{kind:'image', src}` · `{kind:'figure', spec:{data, layout}}` · `{kind:'node'}` · `{kind:'error', message}`.

- [ ] **Step 1: Implementer DOM-halvdelen**

Sett inn i `js/dash.js` etter `autoOrder` og før UMD-halen:

```js
  // ---------- DOM-halvdel (kun browser) ----------
  var _seq = 0;
  var _dashes = {};  // dashId -> {root, grid, overflow, mosaic, used:{}}
  var _cards = {};   // cardId -> {node, content, placed, figEl}
  var _md = null;

  function el(tag, cls, text) {
    var n = document.createElement(tag);
    if (cls) n.className = cls;
    if (text != null) n.textContent = text;
    return n;
  }

  function mdToHtml(text) {
    if (!_md && global.markdownit) _md = global.markdownit({ linkify: true });
    return _md ? _md.render(String(text)) : null;
  }

  function debounce(fn, ms) {
    var t = null;
    return function () {
      var args = arguments;
      clearTimeout(t);
      t = setTimeout(function () { fn.apply(null, args); }, ms);
    };
  }

  function fmtNumber(v) {
    if (typeof v !== 'number' || !isFinite(v)) return String(v);
    var x = Number.isInteger(v) ? v : +v.toFixed(2);
    return x.toLocaleString('nb-NO');
  }

  function themeColor(name, fallback) {
    try {
      var c = getComputedStyle(document.body).getPropertyValue(name).trim();
      return c || fallback;
    } catch (e) { return fallback; }
  }

  D.renderPayload = function (p, nodeEl) {
    var kind = p && p.kind;
    if (kind === 'markdown') {
      var html = mdToHtml(p.text);
      if (html != null) {
        var d = el('div', 'dash-md');
        d.innerHTML = html;
        return d;
      }
      return el('pre', 'dash-text', p.text);
    }
    if (kind === 'text') return el('pre', 'dash-text', p.text);
    if (kind === 'number') {
      var k = el('div', 'dash-kpi');
      k.appendChild(el('span', 'dash-kpi-value', fmtNumber(p.value)));
      if (p.unit) k.appendChild(el('span', 'dash-kpi-unit', p.unit));
      return k;
    }
    if (kind === 'table') {
      var w = el('div', 'dash-table-wrap');
      w.innerHTML = p.html;
      return w;
    }
    if (kind === 'image') {
      var img = el('img', 'dash-img');
      img.src = p.src;
      return img;
    }
    if (kind === 'figure') {
      var f = el('div', 'dash-figure');
      var spec = p.spec || {};
      var layout = Object.assign({
        autosize: true,
        margin: { t: 28, r: 12, b: 36, l: 44 },
        paper_bgcolor: 'rgba(0,0,0,0)',
        plot_bgcolor: 'rgba(0,0,0,0)',
        font: { color: themeColor('--text', '#333') }
      }, spec.layout || {});
      setTimeout(function () {
        if (global.Plotly && f.isConnected) {
          global.Plotly.newPlot(f, spec.data || [], layout,
            { responsive: true, displayModeBar: false });
        }
      }, 0);
      return f;
    }
    if (kind === 'node' && nodeEl) return nodeEl;
    if (kind === 'error') {
      var e = el('div', 'dash-error');
      e.appendChild(el('strong', null, 'Feil: '));
      e.appendChild(el('span', null, p.message));
      return e;
    }
    return el('pre', 'dash-text', JSON.stringify(p));
  };

  function buildControl(spec, report) {
    var wrap = el('label', 'dash-widget');
    wrap.appendChild(el('span', 'dash-widget-label', spec.label || spec.name));
    var input, initial;
    if (spec.type === 'slider') {
      input = el('input');
      input.type = 'range';
      input.min = spec.min; input.max = spec.max;
      if (spec.step != null) input.step = spec.step;
      input.value = spec.default;
      initial = Number(spec.default);
      var out = el('span', 'dash-widget-value', fmtNumber(initial));
      input.addEventListener('input', function () {
        out.textContent = fmtNumber(Number(input.value));
        report(spec.name, Number(input.value));
      });
      wrap.appendChild(input);
      wrap.appendChild(out);
    } else if (spec.type === 'dropdown') {
      input = el('select');
      (spec.options || []).forEach(function (o) {
        input.appendChild(el('option', null, o));
      });
      input.selectedIndex = spec.index || 0;
      initial = spec.index || 0;
      input.addEventListener('change', function () {
        report(spec.name, input.selectedIndex);
      });
      wrap.appendChild(input);
    } else if (spec.type === 'checkbox') {
      input = el('input');
      input.type = 'checkbox';
      input.checked = !!spec.default;
      initial = !!spec.default;
      input.addEventListener('change', function () {
        report(spec.name, input.checked);
      });
      wrap.classList.add('dash-widget--check');
      wrap.insertBefore(input, wrap.firstChild);
    } else if (spec.type === 'numberfield') {
      input = el('input');
      input.type = 'number';
      if (spec.min != null) input.min = spec.min;
      if (spec.max != null) input.max = spec.max;
      if (spec.step != null) input.step = spec.step;
      input.value = spec.default;
      initial = Number(spec.default);
      input.addEventListener('change', function () {
        report(spec.name, Number(input.value));
      });
      wrap.appendChild(input);
    } else { // textfield
      input = el('input');
      input.type = 'text';
      input.value = spec.default != null ? spec.default : '';
      initial = String(input.value);
      input.addEventListener('change', function () {
        report(spec.name, input.value);
      });
      wrap.appendChild(input);
    }
    return { node: wrap, name: spec.name, initial: initial };
  }

  function buildControls(specs, onChange, cls) {
    var bar = el('div', cls);
    var values = {};
    var fire = debounce(function () {
      if (typeof onChange === 'function') onChange(JSON.stringify(values));
    }, 150);
    function report(name, value) { values[name] = value; fire(); }
    specs.forEach(function (spec) {
      var c = buildControl(spec, report);
      values[c.name] = c.initial;
      bar.appendChild(c.node);
    });
    return bar;
  }

  D.create = function (optsJson) {
    var opts = JSON.parse(optsJson || '{}');
    var container = document.getElementById('outputArea');
    var root = el('div', 'dash');
    if (opts.title) {
      var head = el('header', 'dash-header');
      head.appendChild(el('h1', 'dash-title', opts.title));
      root.appendChild(head);
    }
    var mosaic = null;
    if (opts.layout) {
      var m = D.parseMosaic(opts.layout);
      if (m.error) {
        root.appendChild(el('div', 'dash-error', m.error));
      } else {
        mosaic = m;
      }
    }
    var grid = el('div', 'dash-grid');
    if (mosaic) {
      grid.style.gridTemplateAreas = mosaic.gridTemplateAreas;
      grid.style.gridTemplateColumns = 'repeat(' + mosaic.columns + ', 1fr)';
    } else {
      grid.style.gridTemplateColumns = 'repeat(12, 1fr)';
    }
    root.appendChild(grid);
    container.appendChild(root);
    var id = 'dash' + (++_seq);
    _dashes[id] = { root: root, grid: grid, overflow: null, mosaic: mosaic, used: {} };
    return id;
  };

  function placeCard(dash, rec, kind, cols) {
    if (rec.placed) return;
    if (dash.mosaic) {
      var area = rec.area;
      if (!area) {
        area = dash.mosaic.names.find(function (n) { return !dash.used[n]; });
      }
      if (area && dash.mosaic.names.indexOf(area) !== -1) {
        dash.used[area] = true;
        rec.node.style.gridArea = area;
        dash.grid.appendChild(rec.node);
      } else {
        if (!dash.overflow) {
          dash.overflow = el('div', 'dash-grid dash-grid--overflow');
          dash.overflow.style.gridTemplateColumns = 'repeat(12, 1fr)';
          dash.root.appendChild(dash.overflow);
          if (rec.area) console.warn('dash: omraadet "' + rec.area +
            '" finnes ikke i layout; kortet legges under gridet');
        }
        rec.node.style.gridColumn = 'span ' + D.autoSpan(kind, cols);
        dash.overflow.appendChild(rec.node);
      }
    } else {
      rec.node.style.gridColumn = 'span ' + D.autoSpan(kind, cols);
      rec.node.style.order = D.autoOrder(kind);
      dash.grid.appendChild(rec.node);
    }
    rec.placed = true;
  }

  D.addCard = function (dashId, optsJson, onChange, nodeEl) {
    var dash = _dashes[dashId];
    var opts = JSON.parse(optsJson || '{}');
    var card = el('section', 'dash-card');
    if (opts.title) card.appendChild(el('h3', 'dash-cardtitle', opts.title));
    if (opts.controls && opts.controls.length) {
      card.appendChild(buildControls(opts.controls, onChange, 'dash-cardbar'));
    }
    var content = el('div', 'dash-content');
    card.appendChild(content);
    var cardId = 'dashcard' + (++_seq);
    var rec = { node: card, content: content, placed: false,
                area: opts.area || null, dashId: dashId };
    _cards[cardId] = rec;
    if (opts.content) {
      D.updateCard(cardId, JSON.stringify(opts.content), nodeEl); // plasserer også
    } else {
      card.classList.add('dash-card--loading');
      placeCard(dash, rec, 'figure', 0); // foreløpig plassering for funksjonskort
    }
    return cardId;
  };

  D.updateCard = function (cardId, payloadJson, nodeEl) {
    var rec = _cards[cardId];
    if (!rec) return;
    var p = JSON.parse(payloadJson);
    if (rec.figEl && global.Plotly) { try { global.Plotly.purge(rec.figEl); } catch (e) {} }
    rec.content.innerHTML = '';
    var node = D.renderPayload(p, nodeEl);
    rec.figEl = (p.kind === 'figure') ? node : null;
    rec.content.appendChild(node);
    rec.node.classList.remove('dash-card--loading');
    rec.node.classList.toggle('dash-card--error', p.kind === 'error');
    if (rec.dashId && !rec.placed) {
      placeCard(_dashes[rec.dashId], rec, p.kind, p.cols || 0);
    }
  };

  D.addControls = function (dashId, specsJson, onChange) {
    var dash = _dashes[dashId];
    var specs = JSON.parse(specsJson || '[]');
    var bar = buildControls(specs, onChange, 'dash-controls');
    dash.root.insertBefore(bar, dash.grid);
  };
```

Merk: funksjonskort (uten `content`) plasseres umiddelbart med `figure`-spenn og beholder plasseringen — i auto-layout får de dermed alltid 6 kolonner (bevisst enkelt). Statiske kort plasseres av `updateCard` etter korttype.

- [ ] **Step 2: Kjør node-testene — skal fortsatt passere** (DOM-halvdelen refererer `document` kun inne i funksjoner som Node aldri kaller)

Kjør: `node --test tests/js/dash.test.js`
Forventet: PASS, 10 tester.

- [ ] **Step 3: Commit**

```bash
git add js/dash.js
git commit -m "feat(dash): DOM-halvdel - create/addCard/updateCard/addControls, widgets, payload-rendering"
```

---

### Task 4: `css/dash.css` + inkludering

**Files:**
- Create: `css/dash.css`
- Modify: `index.html:11` (legg til `<link>` rett etter `css/dashboard.css`-linjen)

**Interfaces:**
- Consumes: klassenavnene fra Task 3 (`.dash`, `.dash-header`, `.dash-title`, `.dash-grid`, `.dash-grid--overflow`, `.dash-card`, `.dash-card--loading`, `.dash-card--error`, `.dash-cardtitle`, `.dash-cardbar`, `.dash-controls`, `.dash-content`, `.dash-widget`, `.dash-widget--check`, `.dash-widget-label`, `.dash-widget-value`, `.dash-kpi`, `.dash-kpi-value`, `.dash-kpi-unit`, `.dash-md`, `.dash-text`, `.dash-table-wrap`, `.dash-img`, `.dash-figure`, `.dash-error`) og appens temavariabler.

- [ ] **Step 1: Opprett `css/dash.css`**

```css
/* dash v2 — spacing-skala 4/8/16/24, temavariabler fra app.css, ingen hardkodede farger */
.dash { padding: 16px 0; }
.dash-header { margin-bottom: 16px; }
.dash-title { font-size: 1.4rem; font-weight: 650; margin: 0; color: var(--text); }

.dash-controls {
  display: flex; flex-wrap: wrap; gap: 8px 24px;
  padding: 8px 16px; margin-bottom: 16px;
  background: var(--bg-panel); border: 1px solid var(--border); border-radius: 10px;
}

.dash-grid { display: grid; gap: 16px; grid-auto-rows: minmax(96px, auto); }
.dash-grid--overflow { margin-top: 16px; }

.dash-card {
  display: flex; flex-direction: column; min-width: 0;
  background: var(--bg-panel); border: 1px solid var(--border); border-radius: 10px;
  padding: 16px;
  box-shadow: 0 1px 2px color-mix(in srgb, var(--text) 6%, transparent);
}
.dash-card--loading .dash-content {
  min-height: 64px; border-radius: 6px;
  background: color-mix(in srgb, var(--text-muted) 10%, transparent);
  animation: dash-shimmer 1.2s ease-in-out infinite alternate;
}
@keyframes dash-shimmer { from { opacity: .5; } to { opacity: 1; } }
.dash-card--error { border-color: color-mix(in srgb, red 45%, var(--border)); }

.dash-cardtitle {
  margin: 0 0 8px; font-size: .85rem; font-weight: 600;
  text-transform: uppercase; letter-spacing: .04em; color: var(--text-muted);
}
.dash-cardbar {
  display: flex; flex-wrap: wrap; gap: 4px 16px; margin-bottom: 8px;
  padding-bottom: 8px; border-bottom: 1px solid var(--border);
}
.dash-content { flex: 1; min-height: 0; display: flex; flex-direction: column; }

.dash-widget { display: inline-flex; align-items: center; gap: 8px; font-size: .85rem; color: var(--text); }
.dash-widget-label { color: var(--text-muted); }
.dash-widget-value { min-width: 2.5em; text-align: right; font-variant-numeric: tabular-nums; }
.dash-widget input[type="range"] { accent-color: var(--accent); width: 120px; }
.dash-widget select, .dash-widget input[type="text"], .dash-widget input[type="number"] {
  background: var(--bg-code); color: var(--text);
  border: 1px solid var(--border); border-radius: 6px; padding: 4px 8px; font-size: .85rem;
}
.dash-widget input[type="number"] { width: 5em; }
.dash-widget--check input { accent-color: var(--accent); }

.dash-kpi { display: flex; align-items: baseline; gap: 8px; margin: auto 0; }
.dash-kpi-value {
  font-size: 2.2rem; font-weight: 700; line-height: 1.1;
  font-variant-numeric: tabular-nums; color: var(--text);
}
.dash-kpi-unit { font-size: .9rem; color: var(--text-muted); }

.dash-md { color: var(--text); line-height: 1.55; overflow-wrap: break-word; }
.dash-md :first-child { margin-top: 0; }
.dash-text {
  margin: 0; white-space: pre-wrap; font-size: .85rem;
  color: var(--text); overflow: auto; max-height: 40vh;
}

.dash-table-wrap { overflow: auto; max-height: 48vh; }
.dash-table-wrap table { border-collapse: collapse; width: 100%; font-size: .85rem; color: var(--text); }
.dash-table-wrap th {
  position: sticky; top: 0; background: var(--table-header-bg, var(--bg-panel));
  text-align: left; padding: 6px 8px; border-bottom: 2px solid var(--border);
}
.dash-table-wrap td { padding: 4px 8px; border-bottom: 1px solid var(--table-grid, var(--border)); }
.dash-table-wrap tr:nth-child(even) td { background: color-mix(in srgb, var(--text) 3%, transparent); }

.dash-img { max-width: 100%; display: block; margin: auto; }
.dash-figure { flex: 1; min-height: 260px; }

.dash-error {
  padding: 8px 16px; border-radius: 6px; font-size: .85rem;
  color: var(--text);
  background: color-mix(in srgb, red 10%, var(--bg-panel));
  border-left: 3px solid color-mix(in srgb, red 60%, var(--border));
}

@media (max-width: 700px) {
  .dash-grid { grid-template-columns: 1fr !important; grid-template-areas: none !important; }
  .dash-card { grid-area: auto !important; grid-column: auto !important; }
}
```

- [ ] **Step 2: Legg til `<link>` i `index.html`**

Etter linje 11 (`<link rel="stylesheet" href="css/dashboard.css">`):

```html
  <link rel="stylesheet" href="css/dash.css">
```

- [ ] **Step 3: Visuell røyk-sjekk i konsollen**

Start server: `python3 -m http.server 8080` (fra repo-roten), åpne `http://localhost:8080`, kjør i devtools-konsollen:

```js
document.getElementById('outputArea').innerHTML = '';
var id = Dash.create(JSON.stringify({title: 'Test', layout: 'a a b\nc c b'}));
Dash.addCard(id, JSON.stringify({title: 'KPI', content: {kind: 'number', value: 1234.5, unit: 'kr'}, area: 'b'}));
Dash.addCard(id, JSON.stringify({title: 'Tekst', content: {kind: 'markdown', text: '## Hei\nDette er *dash v2*.'}}));
Dash.addCard(id, JSON.stringify({controls: [{type:'slider', name:'n', min:1, max:10, default:3}], title: 'Interaktiv'}), function (v) { console.log('onChange', v); });
```

Forventet: grid med tre kort etter mosaikken, KPI-tall stort, markdown rendret, slider som logger `{"n": <tall>}` (debounced) ved drag. Sjekk både lys og mørk tema (temavelgeren i appen).

- [ ] **Step 4: Commit**

```bash
git add css/dash.css index.html
git commit -m "feat(dash): css/dash.css - designet kortstil, KPI, tabell, widgets, responsiv"
```

---

### Task 5: `brython/dash.py` + registrering i `LIB_REGISTRY`

**Files:**
- Create: `brython/dash.py`
- Modify: `js/brython-engine.js:38-50` (én ny `LIB_REGISTRY`-oppføring)

**Interfaces:**
- Consumes: `window.Dash.create/addCard/updateCard/addControls` nøyaktig som definert i Task 3 (JSON-strenger inn, `onChange(jsonStr)` tilbake, dropdown rapporterer indeks).
- Produces (bruker-API, brukes av eksemplene i Task 6): `dashboard(title="", layout=None)` → `Dash`-objekt med `.add(x, title=None, at=None, unit=None, **kwargs)` og `.controls(**kwargs)`; modulfunksjoner `slider(min, max, step=None, default=None, label=None)`, `dropdown(*options, default=None, label=None)`, `checkbox(default=False, label=None)`, `textfield(default="", label=None)`, `numberfield(default=0, min=None, max=None, step=None, label=None)`.

- [ ] **Step 1: Sjekk figur-deteksjonen i eksisterende kode**

Kjør: `grep -n "figure\|to_plotly_json\|_repr" brython/brython_runner.py brython/plotly_express_brython.py | head -40`

Merk hvilken duck-typing runneren bruker for å oppdage plotly-figurer (embed-markøren `__micro_transform_start_figure__`). `_figure_spec` under skal gjenkjenne samme figurtype — juster metodenavnene i `_figure_spec` hvis runneren bruker andre (f.eks. `.spec`, `.to_dict()`), slik at `pe.histogram(...)`-objekter treffes.

- [ ] **Step 2: Opprett `brython/dash.py`**

```python
"""dash v2 - Brython-adapter. Bygger ALDRI kort/layout-DOM selv;
alt gaar via window.Dash (js/dash.js). Data krysser grensen som JSON."""
from browser import window
import sys
import io
import json


def dashboard(title="", layout=None):
    return Dash(title, layout)


class Widget:
    def __init__(self, kind, values=None, **spec):
        self.kind = kind
        self.spec = {k: v for k, v in spec.items() if v is not None}
        self.values = values  # dropdown: original-objektene (indeks -> verdi)

    def to_spec(self, name):
        d = dict(self.spec)
        d["type"] = self.kind
        d["name"] = name
        return d

    def default(self):
        if self.kind == "dropdown":
            return self.values[self.spec.get("index", 0)]
        if self.kind == "slider":
            return self.spec.get("default", self.spec["min"])
        return self.spec.get("default")

    def from_raw(self, raw):
        """JS-raaverdi -> Python-verdi."""
        if self.kind == "dropdown":
            return self.values[int(raw)]
        if self.kind in ("slider", "numberfield"):
            v = float(raw)
            bounds = [self.spec.get(k) for k in ("min", "max", "step", "default")]
            ints = all(isinstance(b, int) for b in bounds if b is not None)
            return int(v) if ints and v == int(v) else v
        if self.kind == "checkbox":
            return bool(raw)
        return raw


def slider(min, max, step=None, default=None, label=None):
    return Widget("slider", min=min, max=max, step=step,
                  default=default if default is not None else min, label=label)


def dropdown(*options, default=None, label=None):
    if len(options) == 1 and isinstance(options[0], (list, tuple)):
        opts = list(options[0])
    else:
        opts = list(options)
    idx = opts.index(default) if default in opts else 0
    return Widget("dropdown", values=opts,
                  options=[str(o) for o in opts], index=idx, label=label)


def checkbox(default=False, label=None):
    return Widget("checkbox", default=bool(default), label=label)


def textfield(default="", label=None):
    return Widget("textfield", default=str(default), label=label)


def numberfield(default=0, min=None, max=None, step=None, label=None):
    return Widget("numberfield", default=default, min=min, max=max,
                  step=step, label=label)


def _infer(name, value):
    """Implisitt kwarg->widget-mapping (spec 4.2). Rekkefolgen betyr noe:
    bool foer int (bool er subklasse av int)."""
    if isinstance(value, Widget):
        return value
    if isinstance(value, bool):
        return checkbox(default=value)
    if isinstance(value, tuple) and len(value) in (2, 3) \
            and all(isinstance(v, (int, float)) for v in value):
        return slider(*value)
    if isinstance(value, (list, set)) or hasattr(value, "tolist"):
        seq = value.tolist() if hasattr(value, "tolist") else list(value)
        return dropdown(*seq)
    if isinstance(value, str):
        return textfield(default=value)
    if isinstance(value, (int, float)):
        return numberfield(default=value)
    raise ValueError("dash: kan ikke lage kontroll av %s=%r" % (name, value))


def _figure_spec(x):
    """Duck-typet plotly-gjenkjenning (matplotlib-shim er plotly-basert)."""
    for m in ("to_plotly_json", "to_dict"):
        if hasattr(x, m):
            try:
                d = getattr(x, m)()
                if isinstance(d, dict) and "data" in d:
                    return d
            except Exception:
                pass
    if isinstance(x, dict) and "data" in x and "layout" in x:
        return x
    if hasattr(x, "data") and hasattr(x, "layout") and not hasattr(x, "to_html"):
        try:
            return {"data": list(x.data), "layout": dict(x.layout)}
        except Exception:
            pass
    return None


_IMG_EXT = (".png", ".jpg", ".jpeg", ".gif", ".svg", ".webp")


def _payload(x, unit=None):
    """add(x)-dispatch (spec 5). Rekkefolgen er prioritetsrekkefolgen."""
    if x is None:
        return {"kind": "text", "text": ""}
    if isinstance(x, bool):
        return {"kind": "text", "text": str(x)}
    if isinstance(x, (int, float)):
        return {"kind": "number", "value": x, "unit": unit or ""}
    if isinstance(x, str):
        s = x.strip()
        if s.startswith("data:image") or (
                s.split("?")[0].lower().endswith(_IMG_EXT)
                and (s.startswith("http") or "/" in s) and "\n" not in s):
            return {"kind": "image", "src": s}
        return {"kind": "markdown", "text": x}
    fig = _figure_spec(x)
    if fig is not None:
        return {"kind": "figure", "spec": fig}
    if hasattr(x, "to_html"):
        try:
            ncols = len(list(getattr(x, "columns", []) or []))
        except Exception:
            ncols = 0
        return {"kind": "table", "html": x.to_html(), "cols": ncols}
    if hasattr(x, "elt") or hasattr(x, "nodeType"):
        return {"kind": "node"}
    return {"kind": "text", "text": repr(x)}


def _dom_node(x):
    return getattr(x, "elt", x)


def _func_params(f):
    code = f.__code__
    return list(code.co_varnames[:code.co_argcount])


class Dash:
    def __init__(self, title="", layout=None):
        self._cards = {}       # card_id -> dict(func, widgets, unit, last)
        self._shared = {}      # navn -> Widget
        self._shared_vals = {} # navn -> Python-verdi
        self.id = window.Dash.create(json.dumps({"title": title, "layout": layout}))

    # ---- offentlig API ----

    def add(self, x, title=None, at=None, unit=None, **kwargs):
        if callable(x) and not isinstance(x, Widget):
            return self._add_func(x, title, at, unit, kwargs)
        p = _payload(x, unit=unit)
        opts = {"title": title, "area": at, "content": p}
        node = _dom_node(x) if p["kind"] == "node" else None
        return window.Dash.addCard(self.id, json.dumps(opts), None, node)

    def controls(self, **kwargs):
        for name, value in kwargs.items():
            w = _infer(name, value)
            self._shared[name] = w
            self._shared_vals[name] = w.default()
        specs = [w.to_spec(n) for n, w in self._shared.items()]

        def on_change(values_json):
            raw = json.loads(values_json)
            for n, r in raw.items():
                if n in self._shared:
                    self._shared_vals[n] = self._shared[n].from_raw(r)
            for cid, card in self._cards.items():
                if set(card["params"]) & set(self._shared):
                    self._run(cid)

        window.Dash.addControls(self.id, json.dumps(specs), on_change)
        # kort lagt til foer controls(): kjoer paa nytt med delte defaults
        for cid, card in self._cards.items():
            if set(card["params"]) & set(self._shared):
                self._run(cid)

    # ---- internt ----

    def _add_func(self, func, title, at, unit, kwargs):
        widgets = {n: _infer(n, v) for n, v in kwargs.items()}
        specs = [w.to_spec(n) for n, w in widgets.items()]
        card = {
            "func": func,
            "widgets": widgets,
            "unit": unit,
            "params": _func_params(func),
            "vals": {n: w.default() for n, w in widgets.items()},
        }
        holder = {}

        def on_change(values_json):
            raw = json.loads(values_json)
            for n, r in raw.items():
                if n in widgets:
                    card["vals"][n] = widgets[n].from_raw(r)
            self._run(holder["cid"])

        opts = {"title": title, "area": at, "controls": specs, "content": None}
        cid = window.Dash.addCard(self.id, json.dumps(opts),
                                  on_change if specs else None, None)
        holder["cid"] = cid
        self._cards[cid] = card
        self._run(cid)
        return cid

    def _run(self, cid):
        card = self._cards[cid]
        vals = dict(card["vals"])
        for n in card["params"]:
            if n not in vals and n in self._shared_vals:
                vals[n] = self._shared_vals[n]
        buf = io.StringIO()
        old = sys.stdout
        sys.stdout = buf
        node = None
        try:
            res = card["func"](**vals)
            if res is None and buf.getvalue().strip():
                p = {"kind": "text", "text": buf.getvalue().rstrip()}
            else:
                p = _payload(res, unit=card["unit"])
                if p["kind"] == "node":
                    node = _dom_node(res)
        except Exception as e:
            p = {"kind": "error", "message": "%s: %s" % (type(e).__name__, e)}
        finally:
            sys.stdout = old
        window.Dash.updateCard(cid, json.dumps(p), node)
```

- [ ] **Step 3: Registrer modulen i `js/brython-engine.js`**

I `LIB_REGISTRY` (linje 38-50), legg til:

```js
  dash: { aliases: [], deps: [], js: [] },
```

(`dash.py` importerer ingen andre lazy-moduler på modulnivå, så `deps: []`.)

- [ ] **Step 4: Røyk-test i browser**

Med `python3 -m http.server 8080` kjørende: velg Brython-modus i appen og kjør dette scriptet:

```python
import dash
d = dash.dashboard("Adapter-test")
d.add("## Statisk\nMarkdown-kort")
d.add(42, title="Svar", unit="stk")
def dobling(n=(1, 10)):
    return n * 2
d.add(dobling, title="Interaktiv")
```

Forventet: tre kort (markdown, KPI «42 stk», interaktivt kort med slider som viser 2 og oppdateres ved drag). Feil i konsollen → sjekk at JSON-grensesnittet stemmer med Task 3-signaturene.

- [ ] **Step 5: Commit**

```bash
git add brython/dash.py js/brython-engine.js
git commit -m "feat(dash): brython-adapter dash.py - dashboard/add/controls, widget-inferens, dispatch"
```

---

### Task 6: Nye eksempler

**Files:**
- Rewrite: `examples/bry10_dashboard_iris.txt`
- Rewrite: `examples/bry11_dashboard_salg.txt`
- Rewrite: `examples/ex_dashboard_iris.py` og `examples/ex_dashboard_iris.txt` (identisk innhold som bry10 — filnavnene beholdes fordi `dashstatlink/names.json` peker på dem; da trengs ingen registerendring)

**Interfaces:**
- Consumes: bruker-API-et fra Task 5, nøyaktige signaturer.

- [ ] **Step 1: Verifiser at `# load` virker i vanlig Brython-kjøring**

Kjør i appen (Brython-modus):

```python
# load https://raw.githubusercontent.com/hmelberg/safestat/master/data/iris.csv as iris
print(len(iris))
```

Forventet: et antall skrives ut. Hvis `# load` IKKE virker utenfor gammel dashboard-visning: bytt datalinjen i eksemplene til `iris = pd.read_csv("https://raw.githubusercontent.com/hmelberg/safestat/master/data/iris.csv")` (sjekk at `pandas_brython.read_csv` finnes: `grep -n "def read_csv" brython/pandas_brython.py`).

- [ ] **Step 2: Skriv nytt `examples/bry10_dashboard_iris.txt`** (mosaikk + delte kontroller)

```python
#options.mode = brython
#options.title = "Iris-dashboard (dash v2)"
#options.description = "Interaktivt dashboard med mosaikk-layout — uten Pyodide"
# load https://raw.githubusercontent.com/hmelberg/safestat/master/data/iris.csv as iris
import pandas_brython as pd
import plotly_express_brython as pe
import dash

d = dash.dashboard("Iris", layout="""
    antall snitt  snitt
    plot   plot   plot
    plot   plot   plot
    tabell tabell tabell
""")

def utvalg(art, min_lengde):
    sub = iris[iris["species"] == art]
    return sub[sub["sepal_length"] >= min_lengde]

def fordeling(art, min_lengde):
    return pe.histogram(utvalg(art, min_lengde), x="sepal_length", title=art)

d.controls(art=["setosa", "versicolor", "virginica"],
           min_lengde=dash.slider(4, 8, step=1, label="Min. sepal-lengde"))

d.add(lambda art, min_lengde: len(utvalg(art, min_lengde)),
      title="Antall blomster", at="antall")
d.add(lambda art, min_lengde: round(utvalg(art, min_lengde)["sepal_length"].mean(), 2),
      title="Snitt sepal-lengde", unit="cm", at="snitt")
d.add(fordeling, at="plot")
d.add(lambda art, min_lengde: utvalg(art, min_lengde).head(8),
      title="Utvalg", at="tabell")
```

- [ ] **Step 3: Skriv nytt `examples/bry11_dashboard_salg.txt`** (auto-layout, per-kort-kontroller, ingen eksterne data)

```python
#options.mode = brython
#options.title = "Salgs-dashboard (dash v2)"
#options.description = "Auto-layout og kontroller per kort — ingen eksterne data"
import plotly_express_brython as pe
import dash

mnd = ["jan", "feb", "mar", "apr", "mai", "jun"]
salg = {"Nord": [120, 135, 150, 160, 172, 190],
        "Soer": [200, 210, 190, 240, 260, 255]}

d = dash.dashboard("Salg 2026")

d.add("## Salg 2026\nDemo av dash v2 sin auto-layout: KPI-fliser samles "
      "oeverst, plott faar to kolonner, tekst full bredde.")
d.add(sum(salg["Nord"]) + sum(salg["Soer"]), title="Totalt salg", unit="stk")
d.add(max(salg["Soer"]), title="Beste maaned (Soer)", unit="stk")

def kurve(region=["Nord", "Soer"], kumulativ=False):
    verdier = salg[region]
    if kumulativ:
        ut, s = [], 0
        for v in verdier:
            s += v
            ut.append(s)
        verdier = ut
    return pe.line(x=mnd, y=verdier, title=region)

d.add(kurve, title="Utvikling")
d.add(kurve, title="Utvikling (kopi med egne kontroller)")
```

Merk: sjekk `pe.line`-signaturen i `brython/plotly_express_brython.py` (`grep -n "def line" brython/plotly_express_brython.py`) — hvis den krever en DataFrame, bygg en med `pd.DataFrame({"mnd": mnd, "salg": verdier})` og kall `pe.line(df, x="mnd", y="salg", title=region)`.

- [ ] **Step 4: Oppdater `ex_dashboard_iris.py` og `ex_dashboard_iris.txt`**

Kopier innholdet fra Step 2 inn i begge:

```bash
cp examples/bry10_dashboard_iris.txt examples/ex_dashboard_iris.py
cp examples/bry10_dashboard_iris.txt examples/ex_dashboard_iris.txt
```

- [ ] **Step 5: Kjør begge eksemplene i browseren**

Via menyknappene (index.html:95-96). Forventet: bry10 viser mosaikk-dashboard med toppstripe-kontroller som oppdaterer alle fire kort; bry11 viser auto-layout med KPI-er øverst og to uavhengige interaktive plott. Test også `#options`-tittelen, endring av slider/nedtrekk, og en feil (skriv `1/0` i en funksjon midlertidig → rød feilstripe i kortet, resten uberørt).

- [ ] **Step 6: Commit**

```bash
git add examples/bry10_dashboard_iris.txt examples/bry11_dashboard_salg.txt examples/ex_dashboard_iris.py examples/ex_dashboard_iris.txt
git commit -m "feat(dash): eksempler omskrevet til dash v2-API (mosaikk + auto-layout)"
```

---

### Task 7: Slett gammelt dashboard-system

**Files:**
- Delete: `js/dashboard.js`, `css/dashboard.css`, `tests/js/dashboard.test.js`
- Modify: `index.html` — alle koblinger under

**Interfaces:**
- Consumes: ingenting; produserer en kodebase uten `Dashboard`-referanser.

- [ ] **Step 1: Fjern koblingene i `index.html`** (linjenumre var korrekte 2026-07-11, men kan ha forskjøvet seg — bruk grep-ankrene):

1. `<link rel="stylesheet" href="css/dashboard.css">` (linje 11) — slett linjen.
2. `<script src="js/dashboard.js"></script>` (linje 802) — slett linjen.
3. View-dropdown-grenen (linje 4001-4008): blokken `if (v === 'dashboard') { ... }` inkl. `window.__dashViewOverride = true` og `if (window.Dashboard) window.Dashboard.unmount();` — slett hele grenen. Fjern også `<option value="dashboard">` fra view-dropdownens markup (`grep -n 'value="dashboard"' index.html`).
4. `let dashboardPendingParsed = null;` (linje 4170) + kommentarlinjene 4168-4169 — slett.
5. Run-knapp-grenen (linje 9922-9946): fra `dashboardPendingParsed = null;` t.o.m. slutten av `if (window.Dashboard && (activeEditorMode === ...)) { ... }`-blokken — slett.
6. `buildDashboardCtx()` (linje 9827-9899) — slett hele funksjonen.
7. Finally-hooken (linje 10408-10417): `if (dashboardPendingParsed) { ... }` — slett blokken.
8. Linje 10048: `if (activeEditorMode === 'duckdb' && !py && !dashboardPendingParsed)` → fjern `&& !dashboardPendingParsed`.

- [ ] **Step 2: Slett filene**

```bash
git rm js/dashboard.js css/dashboard.css tests/js/dashboard.test.js
```

- [ ] **Step 3: Verifiser at ingenting henger igjen**

```bash
grep -n "Dashboard\.\|dashboardPendingParsed\|__dashViewOverride\|buildDashboardCtx\|dashboard\.js\|dashboard\.css" index.html js/*.js
grep -rn "options.view *= *dashboard\|view=dashboard" examples/ index.html js/*.js
```

Forventet: null treff (bortsett fra evt. `dash.js`/`dash.css` som er de NYE filene — sjekk at treffene bare gjelder dem). Hvis `#options.view`-parsing i index.html har en dashboard-spesialgren (`grep -n "options.view" index.html`), fjern kun dashboard-grenen — `split`/andre visninger beholdes.

- [ ] **Step 4: Kjør gjenværende node-tester + røyk-test**

```bash
for f in tests/js/*.test.js; do node --test "$f" || exit 1; done
```

Forventet: alle PASS. Åpne appen, kjør bry10-eksemplet på nytt + ett vanlig ikke-dashboard-script i Python- og DuckDB-modus (verifiserer at run-stien er hel etter slettingen).

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "refactor(dash): slett gammel dashboard-motor, -visning og -koblinger"
```

---

### Task 8: Helhetlig verifisering

**Files:** ingen endringer (kun verifisering; småfikser committes fortløpende).

- [ ] **Step 1: Alle node-tester**

```bash
for f in tests/js/*.test.js; do node --test "$f" || exit 1; done
```

- [ ] **Step 2: Browser-verifisering** (med `python3 -m http.server 8080`)

Sjekkliste:
1. bry10 (mosaikk): kontroller i toppstripe oppdaterer alle kort; ingen letterboxing i plottet; tabell med sticky header.
2. bry11 (auto-layout): KPI-er øverst, to uavhengige plott-kort med egne kontroller.
3. Output-only-deling: åpne `http://localhost:8080/#output=<raw-url til ex_dashboard_iris.txt>` — dashboardet vises uten editor.
4. Lys + mørk tema: ingen hardkodede farger som skjærer seg (spesielt plott-bakgrunn og tabell).
5. Smal skjerm (<700px): én kolonne.
6. Suksesskriterium 3 (netto linjetall): `git diff --stat <start-commit>..HEAD` — bekreft at slettet ≥ lagt til, eller noter avviket.

- [ ] **Step 3: Push (etter Hans' OK)**

```bash
git push origin HEAD
```

(safestat deployes fra `master`; `git push origin HEAD` er tryggest.)

---

### Task 9: Synk til openstat

**Files (i `/Users/hom/Documents/GitHub/openstat`):**
- Copy: `js/dash.js`, `css/dash.css`, `brython/dash.py`, `js/brython-engine.js`, `tests/js/dash.test.js`, `examples/bry10_dashboard_iris.txt`, `examples/bry11_dashboard_salg.txt`, `examples/ex_dashboard_iris.py`, `examples/ex_dashboard_iris.txt`
- Delete: `js/dashboard.js`, `css/dashboard.css`, `tests/js/dashboard.test.js`
- Modify: `index.html` — samme anker-baserte redigeringer som Task 4 Step 2 og Task 7 Step 1 (openstats index.html er ikke identisk; finn punktene med de samme grep-ankrene)

- [ ] **Step 1: Kopier delte filer**

```bash
cd /Users/hom/Documents/GitHub/openstat
for f in js/dash.js css/dash.css brython/dash.py js/brython-engine.js tests/js/dash.test.js \
         examples/bry10_dashboard_iris.txt examples/bry11_dashboard_salg.txt \
         examples/ex_dashboard_iris.py examples/ex_dashboard_iris.txt; do
  cp "/Users/hom/Documents/GitHub/safestat/$f" "$f"
done
git rm js/dashboard.js css/dashboard.css tests/js/dashboard.test.js
```

- [ ] **Step 2: Rediger openstats `index.html`**

Samme operasjoner som i safestat, funnet via grep (linjenumre avviker):
- Legg til `<link rel="stylesheet" href="css/dash.css">` etter app.css-linken; slett dashboard.css-linken.
- Slett `<script src="js/dashboard.js">`-linjen.
- Kjør Task 7 Step 1 punktene 3-8 med samme grep-ankere; Task 7 Step 3-grepene skal gi null treff etterpå.

Merk UI-prinsippet: openstat = safestat minus funksjoner — ingen openstat-spesifikke unntak skal legges inn; hvis en kobling ikke finnes i openstat, hopp over den.

- [ ] **Step 3: Verifiser og sjekk synk**

```bash
for f in tests/js/*.test.js; do node --test "$f" || exit 1; done
ls sync_check.sh scripts/sync_check.sh 2>/dev/null
```

Kjør `sync_check.sh` hvis den finnes. Browser-røyktest: bry10 + bry11 i openstat lokalt.

- [ ] **Step 4: Commit og push (etter Hans' OK)**

```bash
git add -A
git commit -m "synk fra safestat: dash v2 (ny motor, gammel dashboard slettet)"
git push origin HEAD
```

(openstat deployes fra `main`.)

- [ ] **Step 5: Sjekk dashstatlink**

`names.json` i `hmelberg/dashstatlink` peker på `ex_dashboard_iris.py`/`.txt` — filnavnene er beholdt, så ingen registerendring trengs. Verifiser likevel: åpne `https://openstat.app/#demo` etter deploy (husk ~5 min raw-CDN-cache før ny filinnhold slår gjennom).
